import type { ChecklistSource } from "@/collections/schema";

export const WC_2010_SOURCES: ChecklistSource[] = [
  {
    label:
      "Pending verification — checklist not yet imported. Provide a verified item-level source to upgrade dataStatus.",
  },
];
