import type { AlbumSection } from "@/collections/schema";

// Sections will be populated once the verified item-level checklist is
// available. Until then this list is empty and the album renders as
// metadata-only.
export const WC_2010_SECTIONS: AlbumSection[] = [];
